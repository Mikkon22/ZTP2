{% extends 'base.html.twig' %}

{% block title %}{{ 'nav.transactions'|trans }}{% endblock %}

{% block body %}
<div class="row mb-4">
    <div class="col">
        <h1 class="h3">{{ 'nav.transactions'|trans }}</h1>
    </div>
    <div class="col text-end">
        <a href="{{ path('app_transaction_new') }}" class="btn btn-primary">
            <i class="fas fa-plus me-1"></i>{{ 'transaction.new_transaction'|trans }}
        </a>
    </div>
</div>

<div class="card mb-4">
    <div class="card-body">
        <form method="get" class="row g-3">
            <div class="col-md-3">
                <label for="tag" class="form-label">{{ 'transaction.filter_by_tag'|trans }}</label>
                <select name="tag" id="tag" class="form-select">
                    <option value="">{{ 'transaction.all_tags'|trans }}</option>
                    {% for tag in tags %}
                        <option value="{{ tag.id }}" {% if selected_tag is not null and selected_tag == tag.id %}selected{% endif %}>
                            {{ tag.name }}
                        </option>
                    {% endfor %}
                </select>
            </div>
            <div class="col-md-3">
                <label for="start_date" class="form-label">{{ 'transaction.start_date'|trans }}</label>
                <input type="date" class="form-control" id="start_date" name="start_date" value="{{ start_date }}">
            </div>
            <div class="col-md-3">
                <label for="end_date" class="form-label">{{ 'transaction.end_date'|trans }}</label>
                <input type="date" class="form-control" id="end_date" name="end_date" value="{{ end_date }}">
            </div>
            <div class="col-md-3 d-flex align-items-end">
                <button type="submit" class="btn btn-primary me-2">{{ 'transaction.filter'|trans }}</button>
                <a href="{{ path('app_transaction_index') }}" class="btn btn-secondary">{{ 'transaction.reset'|trans }}</a>
            </div>
        </form>
    </div>
</div>

<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <div class="btn-group">
            <button type="button" class="btn btn-outline-secondary dropdown-toggle" data-bs-toggle="dropdown">
                <i class="fas fa-filter me-1"></i>{% if selected_category %}{{ selected_category }}{% else %}{{ 'transaction.filter_by_category'|trans }}{% endif %}
            </button>
            <ul class="dropdown-menu">
                <li><a class="dropdown-item" href="{{ path('app_transaction_index', app.request.queryString ? app.request.query.all|merge({'category': null}) : {}) }}">{{ 'transaction.all_categories'|trans }}</a></li>
                <li><hr class="dropdown-divider"></li>
                {% for category in categories %}
                    <li><a class="dropdown-item {% if selected_category == category %}active{% endif %}" href="{{ path('app_transaction_index', app.request.queryString ? app.request.query.all|merge({'category': category}) : {'category': category}) }}">{{ category }}</a></li>
                {% endfor %}
            </ul>
        </div>
        
        <div class="btn-group">
            <button type="button" class="btn btn-outline-secondary dropdown-toggle" data-bs-toggle="dropdown">
                <i class="fas fa-wallet me-1"></i>{% if selected_portfolio %}{{ selected_portfolio }}{% else %}{{ 'transaction.filter_by_portfolio'|trans }}{% endif %}
            </button>
            <ul class="dropdown-menu dropdown-menu-end">
                <li><a class="dropdown-item" href="{{ path('app_transaction_index', app.request.queryString ? app.request.query.all|merge({'portfolio': null}) : {}) }}">{{ 'transaction.all_portfolios'|trans }}</a></li>
                <li><hr class="dropdown-divider"></li>
                {% for portfolio in portfolios %}
                    <li><a class="dropdown-item {% if selected_portfolio == portfolio %}active{% endif %}" href="{{ path('app_transaction_index', app.request.queryString ? app.request.query.all|merge({'portfolio': portfolio}) : {'portfolio': portfolio}) }}">{{ portfolio }}</a></li>
                {% endfor %}
            </ul>
        </div>
    </div>
    
    <div class="table-responsive">
        <table class="table table-hover mb-0">
            <thead>
                <tr>
                    <th>{{ 'transaction.date'|trans }}</th>
                    <th>{{ 'transaction.portfolio'|trans }}</th>
                    <th>{{ 'transaction.title'|trans }}</th>
                    <th>{{ 'transaction.category'|trans }}</th>
                    <th>{{ 'transaction.tags'|trans }}</th>
                    <th class="text-end">{{ 'transaction.amount'|trans }}</th>
                </tr>
            </thead>
            <tbody>
                {% for transaction in transactions %}
                    <tr>
                        <td>{{ transaction.date|date('Y-m-d H:i') }}</td>
                        <td>{{ transaction.portfolio.name }}</td>
                        <td>{{ transaction.title }}</td>
                        <td>{{ transaction.category.name }}</td>
                        <td>
                            {% for tag in transaction.tags %}
                                <span class="badge bg-secondary me-1">{{ tag.name }}</span>
                            {% endfor %}
                        </td>
                        <td class="text-end {% if transaction.amount < 0 %}text-danger{% else %}text-success{% endif %}">
                            {{ transaction.amount|number_format(2, '.', ',') }} zł
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="6" class="text-center py-4">{{ 'transaction.no_transactions_found'|trans }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>

{% if transactions|length > 0 %}
    <div class="row mt-4">
        <div class="col-md-6">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">{{ 'transaction.summary'|trans }}</h5>
                    {% set total_income = 0 %}
                    {% set total_expense = 0 %}
                    {% for transaction in transactions %}
                        {% if transaction.amount > 0 %}
                            {% set total_income = total_income + transaction.amount %}
                        {% else %}
                            {% set total_expense = total_expense + transaction.amount %}
                        {% endif %}
                    {% endfor %}
                    
                    <div class="row">
                        <div class="col">
                            <p class="text-muted mb-1">{{ 'transaction.total_income'|trans }}</p>
                            <p class="h4 text-success mb-3">{{ total_income|number_format(2, '.', ',') }} PLN</p>
                            
                            <p class="text-muted mb-1">{{ 'transaction.total_expenses'|trans }}</p>
                            <p class="h4 text-danger mb-3">{{ total_expense|number_format(2, '.', ',') }} PLN</p>
                            
                            <p class="text-muted mb-1">{{ 'transaction.balance'|trans }}</p>
                            <p class="h4 {% if total_income + total_expense < 0 %}text-danger{% else %}text-success{% endif %} mb-0">
                                {{ (total_income + total_expense)|number_format(2, '.', ',') }} PLN
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endif %}
{% endblock %} 